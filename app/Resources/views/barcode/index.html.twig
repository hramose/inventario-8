{% extends 'DGInventarioBundle:Layout:layout.html.twig' %}

{% block contenido %}
    <h1>Barcode list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Codigo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for barcode in barcodes %}
            <tr>
                <td><a href="{{ path('admin_barcode_show', { 'id': barcode.id }) }}">{{ barcode.id }}</a></td>
                <td>{{ barcode.codigo }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_barcode_show', { 'id': barcode.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_barcode_edit', { 'id': barcode.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_barcode_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
