{% extends 'DGInventarioBundle:Layout:layout.html.twig' %}

{% block contenido %}
    <h1>Zona list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Alias</th>
                <th>Descripcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for zona in zonas %}
            <tr>
                <td><a href="{{ path('admin_zona_show', { 'id': zona.id }) }}">{{ zona.id }}</a></td>
                <td>{{ zona.nombre }}</td>
                <td>{{ zona.alias }}</td>
                <td>{{ zona.descripcion }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_zona_show', { 'id': zona.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_zona_edit', { 'id': zona.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_zona_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
