{% extends 'base.html.twig' %}

{% block body %}
    <h1>Barcode</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ barcode.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ barcode.codigo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_barcode_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_barcode_edit', { 'id': barcode.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
