{% extends 'base.html.twig' %}

{% block body %}
    <h1>Rol list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rol in rols %}
            <tr>
                <td><a href="{{ path('admin_rol_show', { 'id': rol.id }) }}">{{ rol.id }}</a></td>
                <td>{{ rol.nombre }}</td>
                <td>{{ rol.descripcion }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_rol_show', { 'id': rol.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_rol_edit', { 'id': rol.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_rol_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
