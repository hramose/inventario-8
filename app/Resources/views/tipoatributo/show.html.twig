{% extends 'base.html.twig' %}

{% block body %}
    <h1>TipoAtributo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipoAtributo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipoAtributo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_tipoatributo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_tipoatributo_edit', { 'id': tipoAtributo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
