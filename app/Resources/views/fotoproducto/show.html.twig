{% extends 'base.html.twig' %}

{% block body %}
    <h1>FotoProducto</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fotoProducto.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ fotoProducto.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_fotoproducto_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_fotoproducto_edit', { 'id': fotoProducto.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
