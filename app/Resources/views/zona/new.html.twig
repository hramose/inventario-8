{#{% extends 'base.html.twig' %}#}

{% block contenido %}
    
    
    {{ form_start(form, {'action': path('admin_zona_new')}) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_zona_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
